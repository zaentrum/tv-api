apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tv-api
  labels:
    app: tv-api
    prometheus: platform
spec:
  selector:
    matchLabels:
      app: tv-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
